# Minimal extension fixture

This private package exists only to exercise source loading, packed-artifact loading, and Pi RPC lifecycle smoke tests. It is not part of the production workspace and must never be published.
