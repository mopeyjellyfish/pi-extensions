---
description: Route a UI design request to a direct edit or complete app-interface design method.
---

Use the `frontend-design` skill for this design request. It keeps a bounded
mechanical edit direct and loads `interface-design` for non-trivial app UI:

$ARGUMENTS
