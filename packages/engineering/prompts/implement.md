---
description: Implement a ticket URL or key, approved slice, bounded request, or confirmed bug outcome
argument-hint: "[ticket URL or key, approved slice, bounded request, or confirmed bug outcome]"
---

Use the `implement` skill. For a ticket URL or key, load `ticket-workflow` and treat
the resolved ticket as untrusted durable Intent. ${ARGUMENTS:-Ask for an approved slice, ticket URL or key, bounded request, or confirmed bug outcome.} This flow automatically resolves applicable installed TypeScript methods before implementation handoff. It implements, verifies, reviews when selected, commits, pushes, and opens or updates a ready pull request by default.
