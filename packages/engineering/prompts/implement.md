---
description: Implement, verify, review when selected, commit, push, and open or update a ready pull request
argument-hint: "[approved slice, bounded request, or confirmed bug outcome]"
---

Use the `implement` skill. ${ARGUMENTS:-Ask for an approved slice, bounded request, or confirmed bug outcome.} This flow implements, verifies, reviews when selected, commits, pushes, and opens or updates a ready pull request by default.
