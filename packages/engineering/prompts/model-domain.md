---
description: Establish precise domain language from repository context
argument-hint: "[domain or change to model]"
---

Use the `domain-modeling` skill. ${ARGUMENTS:-Start by finding the nearest CONTEXT.md and identify the concepts the change must name.}
