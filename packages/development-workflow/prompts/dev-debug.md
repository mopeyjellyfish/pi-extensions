---
description: Diagnose a failure through a tight loop and regression proof
argument-hint: "<symptom or failing command>"
---

# Debug systematically

Load the `pi-systematic-debugging` skill and inspect `development_workflow` status. For: $ARGUMENTS, build a red-capable feedback loop, reproduce and minimise, rank falsifiable hypotheses, instrument discriminating boundaries, prove root cause, observe regression RED and fix GREEN, rerun the original loop, then return to normal review gates.
