---
description: Turn a raw idea, missing capability, or defect into a shaped workflow
argument-hint: "<idea, desired capability, or problem>"
---

# Start development

Load the `pi-development-workflow` skill. Inspect `development_workflow` status, then resolve the workspace for: $ARGUMENTS. For non-trivial work, use Worktrunk status and activate existing work, or ask before creating a feature branch; only then start or adopt the ledger. Read repository truth in that routed workspace, record fresh research evidence, and use repeated Question batches for user-owned decisions. Summarize each batch and shape the pitch before planning slices. Do not approve a gate on the user's behalf.
