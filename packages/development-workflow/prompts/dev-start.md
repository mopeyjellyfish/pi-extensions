---
description: Turn a raw idea, missing capability, or defect into a shaped workflow
argument-hint: "<idea, desired capability, or problem>"
---

# Start development

Load the `pi-development-workflow` skill. Inspect `development_workflow` status, then help the user start or explicitly adopt a workflow for: $ARGUMENTS. Begin with repository truth, batch independent discovery questions with the Question tool, and shape the idea into an agreed pitch before planning slices. Do not approve a gate on the user's behalf.
