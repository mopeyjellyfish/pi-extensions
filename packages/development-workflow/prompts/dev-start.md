---
description: Turn a raw idea, missing capability, or defect into a shaped workflow
argument-hint: "<idea, desired capability, or problem>"
---

# Start development

Load the `pi-development-workflow` skill. Inspect `development_workflow` status, then resolve the workspace for: $ARGUMENTS. For non-trivial work, use Worktrunk status and activate existing work, or ask before creating a feature branch; only then start or adopt the ledger. Read repository truth, create mandatory validated research.md, and perform the bounded prior-art pass by default. Apply the necessity/reuse/stdlib/native/minimum-code simplicity ladder. Request the one Refine-again/Approve-and-continue checkpoint after Discover readiness and consume its exact result in the same interaction. Shape and simplify the pitch before planning slices; use /approve only as compatibility fallback.
